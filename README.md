# FastApi-Project
FastAPI JWT authentication example using OAuth2 and bcrypt. Implements secure login, JWT token generation, and protected routes. Uses a simple in memory database for learning and demonstration.
